#environment switch
#value = [ DEV | TEST | UAT | PREPRO | PRO ];
envType=DEV

proFiles=conf/system/:ENV:/sys_env,conf/system/:ENV:/redis_conf

#value = [ BATCH ]; Is the machine can run the batch-task.